"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Middleware = void 0;
const middleware = require("./middleware/"); // directory index
/**
 * Configurable middleware module.
 *
 * ```javascript
 * import * as OracleBot from '@oracle/bots-node-sdk';
 *
 * export = (app: express.Express): void => {
 *   app.use('/components', OracleBot.Middleware.customComponent({
 *     cwd: __dirname, // root of application source
 *     register: [ // explicitly provide a global registry
 *       './path/to/a/component',
 *       require('./path/to/another/component'),
 *       './path/to/other/components',
 *       './path/to/a/directory',
 *     ]
 *   }));
 * };
 * ```
 */
var Middleware;
(function (Middleware) {
    // export const init = middleware.init;
    Middleware.customComponent = middleware.customComponent;
    class WebhookClient extends middleware.WebhookClient {
    }
    Middleware.WebhookClient = WebhookClient;
    Middleware.WebhookEvent = middleware.WebhookEvent;
    Middleware.webhookReceiver = middleware.webhookReceiver;
})(Middleware = exports.Middleware || (exports.Middleware = {}));
